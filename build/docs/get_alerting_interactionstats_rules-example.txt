import time
import 
from .rest import ApiException
from pprint import pprint

# Configure OAuth2 access token for authorization: PureCloud OAuth
.configuration.access_token = 'YOUR_ACCESS_TOKEN'

# create an instance of the API class
api_instance = .GetAlertingInteractionstatsRulesRequest()
expand = ['expand_example'] # list[str] | Which fields, if any, to expand (optional)

try:
    # Get an interaction stats rule list.
    api_response = api_instance.get_alerting_interactionstats_rules(expand=expand)
    pprint(api_response)
except ApiException as e:
    print("Exception when calling GetAlertingInteractionstatsRulesRequest->get_alerting_interactionstats_rules: %s\n" % e)