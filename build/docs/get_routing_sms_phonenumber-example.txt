import time
import 
from .rest import ApiException
from pprint import pprint

# Configure OAuth2 access token for authorization: PureCloud OAuth
.configuration.access_token = 'YOUR_ACCESS_TOKEN'

# create an instance of the API class
api_instance = .GetRoutingSmsPhonenumberRequest()
address_id = 'address_id_example' # str | Address ID

try:
    # Get a phone number provisioned for SMS.
    api_response = api_instance.get_routing_sms_phonenumber(address_id)
    pprint(api_response)
except ApiException as e:
    print("Exception when calling GetRoutingSmsPhonenumberRequest->get_routing_sms_phonenumber: %s\n" % e)