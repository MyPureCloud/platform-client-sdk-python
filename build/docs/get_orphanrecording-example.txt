import time
import 
from .rest import ApiException
from pprint import pprint

# Configure OAuth2 access token for authorization: PureCloud OAuth
.configuration.access_token = 'YOUR_ACCESS_TOKEN'

# create an instance of the API class
api_instance = .GetOrphanrecordingRequest()
orphan_id = 'orphan_id_example' # str | Orphan ID

try:
    # Gets a single orphan recording
    api_response = api_instance.get_orphanrecording(orphan_id)
    pprint(api_response)
except ApiException as e:
    print("Exception when calling GetOrphanrecordingRequest->get_orphanrecording: %s\n" % e)